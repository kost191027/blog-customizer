# Проектная работа: Вёрстка проекта

## Шаг первый. Изучаем имеющиеся компоненты

[Макет проекта](https://www.figma.com/file/FEeiiGLOsE7ktXbPpBxYoD/Custom-dropdown?type=design&node-id=0%3A1&mode=design&t=eXRJnWC6Xsuw0qR4-1)

Для запуска Storybook выполните:

```
npm run storybook
```

Для запуска линтера для стилей выполните:

```
npm run stylelint
```

Для запуска линтера выполните:

```
npm run lint
```

Для запуска форматтера выполните:

```
npm run format
```

### Функциональные требования

- При нажатии на «стрелку» открывается сайдбар с настройками, при повторном нажатии или клике вне сайдбар закрывается.
- При изменении настроек в сайдбаре они не применяются сразу.
- После нажатия на «применить» стили применяются к статье.
- При нажатии «сбросить» настройки в форме сбрасываются на начальные, которые были при открытии страницы, и стили применяются к статье.
- Настройки устанавливаются через CSS-переменные, которые уже есть в стилях и установлены в коде в дефолтные значения.

## Шаг второй. Реализация формы

Продумайте следующие моменты, прежде чем приступать к коду: 

- как будет организована композиция,
- где вы будете хранить состояние,
- как передавать данные между формой и страницей.

Затем реализуйте механику открытия-закрытия панели с формой, после этого можно будет временно зафиксировать ее пропсом для удобства реализации.

После этого реализуйте форму из имеющихся компонент согласно макету.


## Шаг третий. Обеспечьте передачу данных между формой и страницей

Реализуйте по отдельности сохранение состояния страницы и состояние формы. Обеспечьте применение нового состояния после нажатия на «применить».

---

## ✨ Реализованный функционал

### 🎯 Архитектура состояний

В проекте реализована чёткая архитектура с разделением **состояния формы** и **состояния страницы**:

#### 📋 Состояние формы (`formState`)
- **Расположение:** Компонент `ArticleParamsForm`
- **Назначение:** Хранит текущие значения всех контролов формы
- **Поведение:**
  - Изменяется в реальном времени при взаимодействии пользователя с контролами (выпадающие списки, радио-кнопки)
  - **НЕ влияет** на отображение статьи до момента применения
  - Позволяет пользователю экспериментировать с настройками перед их применением

#### 🎨 Состояние страницы (`articleState`)
- **Расположение:** Компонент `App` (корневой)
- **Назначение:** Хранит активные настройки, применённые к статье
- **Поведение:**
  - Управляет CSS-переменными, которые определяют внешний вид статьи
  - Обновляется **только** при нажатии кнопки "Применить" или "Сбросить"
  - Гарантирует стабильность отображения во время настройки

### 🔄 Механизм передачи данных

```
┌─────────────────────┐
│   App (Родитель)    │
│                     │
│  articleState       │ ← Состояние страницы
│  ↓                  │
│  CSS-переменные     │ → Применяются к <main>
│                     │
│  handleApply()      │ ← Коллбэк для обновления
└──────────┬──────────┘
           │ onApply prop
           ↓
┌─────────────────────┐
│ ArticleParamsForm   │
│                     │
│  formState          │ ← Состояние формы
│  ↓                  │
│  Контролы формы     │ → Select, RadioGroup
│                     │
│  handleSubmit()     │ → Вызывает onApply(formState)
└─────────────────────┘
```

### ⚙️ Реализованные возможности

#### 🎛️ Настройки кастомизации

Пользователь может настроить следующие параметры статьи:

| Параметр | Опции | Тип контрола |
|----------|-------|-------------|
| **Шрифт** | Open Sans, Ubuntu, Cormorant Garamond, Days One, Merriweather | Выпадающий список |
| **Размер шрифта** | 18px, 25px, 38px | Радио-кнопки |
| **Цвет шрифта** | Чёрный, Белый, Серый, Розовый, Ярко-розовый, Жёлтый, Зелёный, Голубой, Фиолетовый | Выпадающий список |
| **Цвет фона** | Белый, Чёрный, Серый, Розовый, Ярко-розовый, Жёлтый, Зелёный, Голубой, Фиолетовый | Выпадающий список |
| **Ширина контента** | Широкий (1394px), Узкий (948px) | Радио-кнопки |

#### 🎬 Интерактивное поведение

1. **Открытие/закрытие панели настроек**
   - Клик по кнопке-стрелке переключает видимость сайдбара
   - Клик вне области сайдбара автоматически закрывает панель
   - Используется кастомный хук `useOutsideClickClose` для отслеживания кликов

2. **Применение настроек**
   ```typescript
   // При нажатии "Применить"
   handleSubmit → onApply(formState) → setArticleState(newState)
   ```
   - Текущее состояние формы передаётся в родительский компонент
   - Обновляются CSS-переменные на элементе `<main>`
   - Статья мгновенно перерисовывается с новыми стилями

3. **Сброс настроек**
   ```typescript
   // При нажатии "Сбросить"
   handleReset → setFormState(default) + onApply(default)
   ```
   - Форма возвращается к начальным значениям (`defaultArticleState`)
   - Статья также возвращается к исходному виду
   - Оба состояния синхронизируются

### 🛠️ Технические детали реализации

#### CSS-переменные

Стилизация управляется через CSS-переменные, установленные на корневом элементе `<main>`:

```typescript
style={{
  '--font-family': articleState.fontFamilyOption.value,
  '--font-size': articleState.fontSizeOption.value,
  '--font-color': articleState.fontColor.value,
  '--container-width': articleState.contentWidth.value,
  '--bg-color': articleState.backgroundColor.value,
} as CSSProperties}
```

Это позволяет:
- ✅ Централизованно управлять темой
- ✅ Избежать prop drilling
- ✅ Мгновенно применять изменения ко всем вложенным компонентам
- ✅ Использовать нативные возможности CSS

#### Типизация

Все состояния строго типизированы через TypeScript:

```typescript
export type ArticleStateType = {
  fontFamilyOption: OptionType;
  fontColor: OptionType;
  backgroundColor: OptionType;
  contentWidth: OptionType;
  fontSizeOption: OptionType;
};
```

#### Константы и начальные значения

Все доступные опции и значения по умолчанию вынесены в `src/constants/articleProps.ts`:
- Массивы опций для каждого типа настроек
- Объект `defaultArticleState` с начальными значениями
- Единый источник правды для всего приложения

### 📦 Структура компонентов

```
src/
├── index.tsx                          # App с состоянием страницы
├── components/
│   ├── article/
│   │   └── Article.tsx                # Статья (читает CSS-переменные)
│   └── article-params-form/
│       └── ArticleParamsForm.tsx      # Форма с состоянием формы
├── ui/
│   ├── select/                        # Выпадающий список
│   ├── radio-group/                   # Группа радио-кнопок
│   ├── button/                        # Кнопки "Применить" и "Сбросить"
│   └── arrow-button/                  # Кнопка открытия панели
└── constants/
    └── articleProps.ts                # Опции и дефолтные значения
```

### 🎓 Ключевые концепции React

Проект демонстрирует следующие паттерны и практики:

- **Lifting State Up** — состояние поднято на уровень `App` для управления CSS-переменными
- **Controlled Components** — все инпуты полностью контролируются React-состоянием
- **Composition** — переиспользуемые UI-компоненты (`Select`, `RadioGroup`, `Button`)
- **Custom Hooks** — `useOutsideClickClose` для закрытия панели при клике вне её области
- **TypeScript** — полная типизация props, state и констант
- **Separation of Concerns** — чёткое разделение состояния формы и состояния приложения

### 🚀 Запуск проекта

Установка зависимостей:
```bash
npm ci
```

Запуск dev-сервера:
```bash
npm start
```

Приложение откроется по адресу [http://localhost:8080](http://localhost:8080)

---

**Проект готов к использованию и демонстрации! 🎉**
